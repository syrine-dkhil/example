<server description="Sample Open Liberty server">
    <featureManager>
        <feature>mpConfig-1.3</feature>
        <feature>mpHealth-2.1</feature>
        <feature>mpMetrics-2.2</feature>
        <feature>webProfile-8.0</feature>
    </featureManager>

    <applicationManager autoExpand="true"/>

    <mpMetrics authentication="false"/>

    <variable name="default.http.port" defaultValue="9080"/>
    <variable name="default.https.port" defaultValue="9443"/>
    <variable name="app.context.root" defaultValue="/"/>
    <variable name="db.jdbc.url" defaultValue="jdbc:h2:mem:defaultdb;LOG=1;USER=sa;password=sa"/>

    <httpEndpoint host="*" httpPort="${default.http.port}" httpsPort="${default.https.port}" id="defaultHttpEndpoint"/>
    <keyStore id="defaultKeyStore" password="OpenLiberty"/>

    <webApplication location="example.war" contextRoot="${app.context.root}"/>

    <library id="h2">
        <fileset dir="${shared.resource.dir}" includes="h2*.jar"/>
    </library>

    <dataSource id="DefaultDataSource" jndiName="jdbc/DefaultDataSource">
        <jdbcDriver libraryRef="h2"/>
        <properties URL="${db.jdbc.url}" password="sa" user="sa"/>
    </dataSource>
</server>
